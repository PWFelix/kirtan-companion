# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — Supabase keys live here. The template (.env.example) IS committed.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local reference material (not for the repo or the shipped site)
reference/

# Local Claude Code agent definitions (keep on disk, not in the repo)
.claude/code-reviewer.md
